<div class="column">

<h4>Add Player</h4>
<p>Currently manually adding available players. This will be deleted in favor of a crawler.</p>

  <%= form_for @player, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="form-group">
  <%= f.text_field :full_name, class: 'form-input' %>
  </div>
  <%= f.submit class: 'btn btn-primary input-group-btn' %>
  <% end%>
  </div>
</form>

</div>